# Data

Document every dataset with its source, license, collection date, fields, and whether records are real or synthetic. Local CSV files are ignored by default to prevent accidental commits of large or sensitive data.
